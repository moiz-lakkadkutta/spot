# Spot — Fire OS (Expo)

`pnpm expo` then press `a` with the stick connected (`adb connect <ip>`), or `pnpm --filter @spot/expo build:tv` for an APK. Leanback launcher intent is set so the app shows on the Fire TV home.
